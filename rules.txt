MODULO DE ECOMMERCE

EL MODULO ECOMMERCE NOS PERMITIRA ADMINISTRAR TANTO LAS ORDENES COMO LOS PRODUCTOS DE LA PLATAFORMA
se divide en 2 grandes secciones la vista para admin con el rol de FAC y la vista para usuarios con el rol de CLI

tenemos la vista para admin (FAC) que a su vez tiene los siguientes modulos
- Listado de productos con filtros y paginacion (ya lo hicimos) -> /dashboard/(facturacion)/fac-ecommerce/productos
- Crear product (ya lo hicimos) -> /dashboard/(facturacion)/fac-ecommerce/productos/nuevo
- Detalle de producto (ya lo hicimos) -> /dashboard/(facturacion)/fac-ecommerce/productos/detalle/:id
- Listado de ordenes con filtros y paginacion (ya lo hicimos) -> /dashboard/(facturacion)/fac-pedidos
- Detalle de orden (ya lo hicimos) -> /dashboard/(facturacion)/fac-pedidos/detalle/:id

tenemos la vista para usuario (CLI) que a su vez tiene los siguientes modulos
- Listado de ordenes (ya lo hicimos) -> /dashboard/(cliente)/cli-pedidos
- Detalle de orden (ya lo hicimos) -> /dashboard/(cliente)/cli-pedidos/detalle/:id
- Listado de productos (ya lo hicimos) -> /dashboard/(cliente)/cli-tienda
- Detalle de producto (ya lo hicimos) -> /dashboard/(cliente)/cli-tienda/detalle/:id
- Pagina de carrito (pendiente) -> /dashboard/(cliente)/cli-tienda/carrito

PAGINA DE CARRITO (CLI)
1. listar todos los productos del carrito
2. Que haya un  boton procesar venta que por ahora solo muestre un alert de "procesar venta"

- usar los componentes de shared si es necesario
- mantener los colores del Proyecto de styles.css
- mantener la estructura que vengo manejando

IMPORTANTE NO HACER MAS COSAS DE LAS QUE TE PIDO
