Quiero que realicemos la siguiente vista:
/dashboard/(cliente)/cli-unilevel/lotes-disponibles

para ello lo primero que haremos sera consumir el servicio de getProjects de src/features/lots/services/lotsService.ts
y mostraremos en cards todos los proyectos que nos devuelva el servicio, al hacer click en una card, en la misma vista
mostraremos los lotes disponibles de ese proyecto, para ello usaremos el servicio getAllLots de src/features/lots/services/lotsService.ts

luego usaremos los componentes de src/features/shared tanto para la paginacion como para la tabla de lotes, tambien recuerda que para
mobile usaremos cards en lugar de una tabla. tomar de referencia la vista de src/app/dashboard/(cliente)/cli-transacciones/mis-pagos/page.tsx

la api de getAllLots recibe parametros para filtrar, ordenar y paginar, estos parametros son:
- page
- limit
- term (para buscar por nombre de lote)
- stageId (para filtrar por etapa) se debe consumir la api getStages para obtener las etapas de un proyecto
- blockId (para filtrar por manzana) se debe consumir la api getBlocks para obtener las manzanas de una etapa

se debe poder regresar a la seleccion de proyecto, para ello usaremos un boton de regresar que nos llevara a la vista inicial
recuerda crear los componentes que creas necesarios y usar los componentes ya existentes en src/features/shared
tambien usar los colores de globales de src/styles/globals.css
